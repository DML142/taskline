version: "2"
sql:
  - engine: postgresql
    queries: queries
    schema: migrations
    gen:
      go:
        package: database
        out: internal/platform/database/sqlc
        sql_package: pgx/v5
